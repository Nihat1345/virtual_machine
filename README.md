# virtual_machine
A virtual machine that I experiment with it is is not complete yet I will ad a debugger so most of the debug functions that are commented will be implemented later and interrupt functions are disabled right now the test.RUN file is a just a halt file
